/**
 * Runs gitnoob.
 * @param {string} cwd Current working directory.
 * @param {string[]} argv Command line arguments.
 */
function gitnoob(cwd, argv) {
  console.log('gitnoob', cwd, argv);
}

module.exports = gitnoob;
